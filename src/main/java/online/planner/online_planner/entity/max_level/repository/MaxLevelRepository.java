package online.planner.online_planner.entity.max_level.repository;

import online.planner.online_planner.entity.max_level.MaxLevel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MaxLevelRepository extends JpaRepository<MaxLevel, Long> {
}
